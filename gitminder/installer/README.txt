Must install qt installer framework!

When creating a new release, create as a "Release", not a "Debug"

git2.dll and gitminder.exe are currently the only two files created by me, specifically

C:\Users\Shook\Documents\git\gitminder\gitminder\installer>binarycreator.exe -c config\config.xml -p packages gitminder.exe